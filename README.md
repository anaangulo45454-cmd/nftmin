# VRAM Miner

Vite + React single-page VRAM Miner interface, designed from the supplied visual references. Ready for GitHub and Vercel.

## Run
npm install
npm run dev

## Build
npm run build

## Deploy
Import the repository into Vercel. Framework: Vite (auto-detected).

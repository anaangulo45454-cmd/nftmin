# Video reference

The supplied screen recording was used as a visual reference for the scroll behavior and lower-page layout.
The recording is approximately 1.7 seconds long and shows the page already scrolled into the hardware/drop/on-chain sections.
The implementation keeps the ticker and navigation visible while the page scrolls, matching the reference behavior.
